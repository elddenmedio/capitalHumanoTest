import { MomentLocalPipe } from './moment-local.pipe';

describe('MomentLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
